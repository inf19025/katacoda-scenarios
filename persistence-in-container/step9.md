Wie bereits am Anfang angemerkt, ist der Aufbau der Podman CLI gleich zu der CLI von Docker.

Daher können wir, wie in Docker mit `podman run -it centos:latest`{{execute}} einen Container mit Podman starten.
